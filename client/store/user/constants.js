export const NAME = 'user';

export const LOGIN = `${NAME} / login`;
export const SIGNUP = `${NAME} / sign up`;
export const LOGOUT = `${NAME} / logout`;
export const GET_DATA = `${NAME} / get data`;
